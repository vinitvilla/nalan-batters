# Local Development Environment - Example
# Copy this file to .env.local (or .env) and fill in real values.
# Do NOT commit real secrets to source control.

# App & API
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Google Maps (used for Maps JS API, Geocoding, Places)
# Get a key from: https://console.cloud.google.com/apis/credentials
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Firebase (client keys for frontend use)
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_firebase_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id

# Server / Admin Firebase credentials (sensitive - keep in secure env)
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project.iam.gserviceaccount.com
# For FIREBASE_PRIVATE_KEY keep the multiline key, escaped newlines (\n) if storing in a single-line env file.
# Example placeholder below; replace entire value with your actual private key (do NOT commit).
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# Database (Prisma) - example Postgres URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://postgres@localhost:5432/nalan-batters?schema=public

# Routing / Optimization APIs (optional)
NEXT_PUBLIC_OPENROUTESERVICE_API_KEY=your_openrouteservice_api_key_here

# NOTE:
# - Keep real secret values out of version control. Use a .env.local (gitignored) or a secrets manager for CI/production.
# - For multiline secrets (like private keys), some platforms require escaping newlines as \n.
# - If you need additional service keys (Mapbox, GraphHopper, Stripe, SMTP, NEXTAUTH, etc.), add them as needed following these patterns.
